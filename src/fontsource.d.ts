// Side-effect CSS import, this package ships no type declarations (unlike @fontsource-variable/archivo).
// Keep this file free of top-level import/export so the declaration stays ambient.
declare module '@fontsource-variable/jetbrains-mono';
